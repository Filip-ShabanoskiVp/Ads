@model IEnumerable<Oglasi.Models.Kategorija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BrojKat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BrojNaOglasi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BrojKat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrojNaOglasi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ImeKat">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ImeKat">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ImeKat">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
